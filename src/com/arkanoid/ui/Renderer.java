package com.arkanoid.ui;

import com.arkanoid.core.GameObject;

public class Renderer {
    public void draw(GameObject obj) {
        // Logic để vẽ một GameObject.
        // Ở phiên bản terminal, nó sẽ in ký tự ra console.
        // Ở phiên bản GUI, nó sẽ vẽ hình ảnh lên panel.
    }

    public void clearScreen() {
        // Logic xóa màn hình để chuẩn bị cho khung hình mới
    }
}
